# A6 bounded pilot implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Freeze the approved pilot's operational definitions, execute at most 14 natural SIM attempts, and stop for review without changing A1–A5.

**Architecture:** A6 imports the frozen A5 core and ROS adapter. A thin policy selector chooses existing task/generic rankings (or the two approved ablations); a separate recorder measures public TF trajectories and simulation-time events. Plain scene JSON and individual run directories are sufficient; no automatic retry, formal matrix, or new runtime framework.

**Tech Stack:** Existing Python 3.10 numerical environment, ROS Noetic Python 3.8 adapter, numpy, unittest, existing SIM Gazebo launcher and physical checker.

---

## Checkpoint before execution

- [x] Operational protocol, common parameters, restricted-randomized 14-slot schedule and three seeds documented.
- [x] Task 2 numerical policy/worker implemented with RED/GREEN tests; uniform budget spec review passed at `7b0a01b`.
- [x] Necessary read-only RGB-D admission calculation implemented and tested; all three listed seeds fail at the proposed initial view.
- [ ] ROS adapter, simulation-time recorder, complete obstacle/template qualification and physical pilot execution remain unimplemented/unrun.

The approved design's common-sensing setup review condition applies. No attempt
has been activated. A request to revise only the shared initial scene setup is
pending; do not implement a silent new setup or start the listed slots. The
tasks below remain the resume plan, not a claim that the runner is ready.

## Task 1: Freeze protocol and scene slots before execution

Files: `docs/A6_PILOT_PROTOCOL.md`, `configs/a6_pilot.json`.

- [x] Record exact window/vote/stopping/selection/invalid/seed/time rules in the protocol. Latest user instructions supersede the earlier design's wall-time efficiency and possible development reruns.
- [x] Store three scene descriptions and exactly 14 ordered slots. Easy Fixed-view goes first to check the common sensing setup; a seeded shuffle of the other three methods supplies the first restricted-random order, then tiers use cyclic rotations. This descriptive pilot has a deliberate Fixed-first exception, not unrestricted random ordering. No result-dependent seed search or replacement.
- [ ] Validate JSON and method-blind scene geometry before any runtime. Sensor/bootstrap or tier-qualification failure stops remaining pilot for review, not parameter tuning.
- [ ] Commit the protocol, configuration and implementation before attempt 1.

## Task 2: Minimal pure A6 policy and worker

Files: `src/a6_pilot/__init__.py`, `src/a6_pilot/policy.py`, `src/a6_pilot/worker.py`, `scripts/a6_core_worker.py`, `tests/test_a6_policy.py`.

- [x] Write failing tests using the existing `tests.test_a5_core.inputs/scan/ground_points` fixtures and the real A2 mapper. Required contracts:

```python
ours, ranking = decide_policy(field, raw, belief, current, method='ours', round_count=1, config=config)
reference, original_ranking = a5_decide(field, raw, belief, current, round_count=1, config=config)
assert ours == reference
generic, shared = decide_policy(field, raw, belief, current, method='generic', round_count=1, config=config)
assert [c.viewpoint for c in shared.candidates] == [c.viewpoint for c in ranking.candidates]
assert generic['next_viewpoint'] == [*shared.best_generic.viewpoint.position_xyz, shared.best_generic.viewpoint.yaw_rad]
```

- [x] Run `PYTHONPATH=src <core-python> -m unittest tests.test_a6_policy -v`, verify failure from missing A6 implementation.
- [x] Implement `decide_policy(..., method, round_count, config)` returning `(choice, ranking)`. Call frozen A5 `decide` first after A6 argument checks. Ours returns its exact result. Generic reuses `generic_order` and checks its own positive gain/score, not global task status. Fixed returns current/stay until window 3 regardless of gain. Same highest-relevance/first-tie confirmed candidate is never replaced. `no_cost` uses dataclass replacement of A5Config flight_weight=0. `no_occlusion` uses A4 `range_fov`, retaining candidate validity, occupied-target exclusion, A3 and belief. Preserve base ranking diagnostics, put variant selected score/gain in explicit choice fields. Unknown method raises ValueError.
- [x] Implement `rm4d_top_one(raw)` returning the first `raw['candidates']` exact x/y/yaw/ID and original ranking fields; no A2/catalog fallback. Empty candidates means no handoff, never invent a pose.
- [x] Worker imports frozen `initialize`, `make_field`, `replay_observations`, output functions. Init forwards unchanged after uniform A6 budget validation; observe reads the same ordered NPZ history and uses A6 selector; add `rm4d_select` operation with no cloud. Save each round's real fields/ranking/choice through existing output functions.
- [x] Verify frozen-equivalent Ours, Generic positive gain with no task support, common budget/score precedence, Fixed exactly 3 windows, no first-confirmed early stop, occupied/clipped/exact tie behavior, ablation isolation, top-1 without A2. Commit only these task files.
- [x] Independent spec review, then code-quality review; address material findings. Checkpoint review passed; not a pilot-ready verdict.

## Task 3: ROS adapter and simulation-time measurements

Files: `scripts/run_a6_sim.py`, `scripts/a6_metrics.py`, `tests/test_a6_adapter.py`, `tests/test_a6_metrics.py`.

- [ ] Write failing tests for method option routing, unchanged A5 defaults, event classification, exact Ground handoff and simulation-time resource arithmetic. Example: events start at sim 10, stop at sim 25 and wall 100..140 must yield T_task=15, not 40. A trajectory [(0,0,0),(3,0,0),(3,4,0)] yields 7 m; a missing TF sample yields incomplete distance rather than zero-filled travel.
- [ ] Run each test and verify RED.
- [ ] Build a subclass of frozen A5 adapter. All MID360 methods inherit its full air loop, only `_a5_core` routes to the A6 worker. RM4D-only explicitly shares initial flight/RGBD/query/landing and omits the observation loop. `_execute_pregrasp` publishes `A6_EXEC_READY` only after the refined-grasp branch returns successfully. No change to MoveIt or controllers.
- [ ] Record status synchronously to JSONL (also publish existing status), simulation-time boundaries for capture/active/landing/task/stages, and public map UAV/Ground TF every 0.1 simulation seconds with gaps marked. Keep wall duration for runtime/compute diagnostics only. Arm/gripper wrappers record stage boundaries without changing execution.
- [ ] Use the inherited physical checker as an independent outcome measurement; do not expose truth to the policy. Record failed/aborted attempts and original reasons. Stop physical commands through existing cleanup paths.
- [ ] Run ROS-free unit tests and ROS `--check-imports` without starting a task; review the integration before execution.

## Task 4: Scene setup and preflight qualification

Files: `scripts/a6_scene.py`, `tests/test_a6_scene.py`, `configs/a6_pilot.json`.

- [ ] Test box SDF dimensions/pose, Ground/target spawn argument mapping and method-blind geometry checks before implementation.
- [ ] Reuse A5 runtime launcher with SIM's existing target/BUNKER pose arguments. Spawn static ground-supported 1 m boxes through Gazebo before task start only. No teleport/reset during an attempt. Runtime policies receive no scene truth.
- [ ] Qualify the nominal task patch with the existing saved nominal A1 support for setup diagnostics; live initial query remains authoritative. Check sensor range/FOV, actual oriented-box intersections, at least one nominal-clear exact footprint and coarse Ground path. Actual first-window unknown areas are manipulation checks, not outcome-based exclusions.
- [ ] Keep scene labels/templates fixed and report qualification discrepancies; do not tune scenes after viewing method outcomes.

## Task 5: Run the bounded pilot and review

Files: `outputs/a6/pilot-20260908/*`, `docs/A6_PILOT_RESULTS.md`.

- [ ] Run full unittest suite; verify no frozen A1–A5 file changes. Commit the executable protocol before runtime.
- [ ] Execute slots serially, independent fresh Gazebo initialization, one Ground candidate attempt, original per-stage guards and common 1200 wall-second outer guard. Record slot activation even for setup-invalid runs. Never retry/recycle a slot or exceed 14 attempts.
- [ ] Inspect Easy common bootstrap and fixed-view observation feasibility before later slots. Stop for review if common sensing geometry is untenable. Otherwise complete all 14 slots, retaining every failure.
- [ ] Summarize per-trial physical success, D_exec, C_env, actual windows/moves/path/time, stage outcomes and failure/invalid status. Report complete Ours/Generic pairs and discordants; no significance/advantage claim from this pilot. Formal plan uses paired binary exact McNemar, sole primary comparison Ours–Generic.
- [ ] Review results and code; commit a bounded handoff. Stop for user review. No formal run or frozen tuning.
